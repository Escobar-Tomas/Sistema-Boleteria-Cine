<UserControl x:Class="CapaPresentacion_WPF.Vistas.VistaSalas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" Background="White">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#F8F9FA" Padding="20" CornerRadius="8" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="🏢 Nueva Sala" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2C3E50"/>

                <TextBlock Text="Nombre de la Sala:" Foreground="Gray"/>
                <TextBox Text="{Binding NombreSala}" Padding="8" Margin="0,5,0,15"/>

                <TextBlock Text="Capacidad (Personas):" Foreground="Gray"/>
                <TextBox Text="{Binding CapacidadSala}" Padding="8" Margin="0,5,0,20"/>

                <Button Content="Guardar Sala" Command="{Binding GuardarCommand}"
                        Padding="10" Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Margin="20,0,0,0">
            <DockPanel>
                <TextBlock Text="Listado de Salas" DockPanel.Dock="Top" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>

                <DataGrid ItemsSource="{Binding ListaSalas}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="35" BorderBrush="#E0E0E0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacidad}" Width="100"/>

                        <DataGridTemplateColumn Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Eliminar" 
                                            Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            Background="#E74C3C" Foreground="White" BorderThickness="0" Padding="5,0" Margin="2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>