<UserControl x:Class="CapaPresentacion_WPF.Vistas.VistaVentas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200" Background="Transparent">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="7.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Direction="270" ShadowDepth="5" Color="#E0E0E0"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#2C3E50" CornerRadius="10,10,0,0" Padding="20">
                    <StackPanel>
                        <TextBlock Text="CINE SYSTEM" Foreground="White" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock Text="Ticket de Venta" Foreground="#BDC3C7" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Padding="25">
                    <StackPanel>
                        <TextBlock Text="Película Seleccionada" Foreground="#7F8C8D" FontSize="12" Margin="0,0,0,5"/>
                        <TextBlock Text="{Binding FuncionSeleccionada.Pelicula.Titulo, FallbackValue='Seleccione una función'}" 
                                   FontSize="16" FontWeight="Bold" TextWrapping="Wrap" Foreground="#2C3E50" Margin="0,0,0,20"/>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Sala" Foreground="#7F8C8D" FontSize="12"/>
                                <TextBlock Text="{Binding FuncionSeleccionada.Sala.Nombre}" FontWeight="SemiBold"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock Text="Hora" Foreground="#7F8C8D" FontSize="12" HorizontalAlignment="Right"/>
                                <TextBlock Text="{Binding FuncionSeleccionada.FechaHoraInicio, StringFormat=HH:mm}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#E0E0E0" Margin="0,0,0,20"/>

                        <TextBlock Text="Cantidad de Entradas" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                            <Button Command="{Binding DisminuirEntradasCommand}" Content="-" Width="40" Height="40" Background="#ECF0F1" Foreground="#2C3E50" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <TextBlock Text="{Binding CantidadEntradas}" FontSize="24" FontWeight="Bold" Margin="20,0" VerticalAlignment="Center"/>

                            <Button Command="{Binding AumentarEntradasCommand}" Content="+" Width="40" Height="40" Background="#ECF0F1" Foreground="#2C3E50" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Padding="25" Background="#F9F9F9" CornerRadius="0,0,10,10">
                    <StackPanel>
                        <DockPanel LastChildFill="False" Margin="0,0,0,15">
                            <TextBlock Text="Total a Pagar" FontSize="16" Foreground="#7F8C8D" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalVenta, StringFormat=C}" DockPanel.Dock="Right" FontSize="26" FontWeight="Bold" Foreground="#27AE60"/>
                        </DockPanel>

                        <Button Command="{Binding FinalizarVentaCommand}" Content="CONFIRMAR PAGO" Height="50" Background="#2980B9" Foreground="White" FontWeight="Bold" FontSize="14" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#3498DB"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Funciones Disponibles Ahora" FontSize="22" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

            <ListBox ItemsSource="{Binding ListaFunciones}" SelectedItem="{Binding FuncionSeleccionada}" 
                     Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="0,0,15,15"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Card" Background="White" CornerRadius="10" Width="220" Cursor="Hand" Margin="0,0,10,10">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Direction="270" Color="#EAEAEA"/>
                                        </Border.Effect>
                                        <Grid>
                                            <ContentPresenter/>
                                            <Border x:Name="SelectedBorder" Background="#2980B9" Opacity="0" CornerRadius="10" IsHitTestVisible="False"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Card" Property="Background" Value="#EBF5FB"/>
                                            <Setter TargetName="Card" Property="BorderBrush" Value="#3498DB"/>
                                            <Setter TargetName="Card" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Border Height="120" CornerRadius="10,10,0,0" Background="#34495E">
                                <TextBlock Text="🎬" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
                            </Border>

                            <Border Padding="15">
                                <StackPanel>
                                    <TextBlock Text="{Binding Pelicula.Titulo}" FontWeight="Bold" FontSize="14" TextTrimming="CharacterEllipsis" Foreground="#2C3E50" ToolTip="{Binding Pelicula.Titulo}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="🕒" FontSize="11" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding FechaHoraInicio, StringFormat=HH:mm}" FontSize="12" Foreground="#7F8C8D"/>

                                        <TextBlock Text="📍" FontSize="11" Margin="10,0,5,0"/>
                                        <TextBlock Text="{Binding Sala.Nombre}" FontSize="12" Foreground="#7F8C8D"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding PrecioTicket, StringFormat=C}" FontWeight="Bold" Foreground="#27AE60" Margin="0,10,0,0" FontSize="16"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>