<UserControl x:Class="CapaPresentacion_WPF.Vistas.VistaVentas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:conv="clr-namespace:CapaPresentacion_WPF.Convertidores"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F5F5F5">
    <UserControl.Resources>

        <conv:PosterUrlConverter x:Key="MiPosterConverter"/>

        <Style x:Key="FunctionCardStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="CardBorder" 
                                Background="White" 
                                CornerRadius="8" 
                                BorderThickness="2" 
                                BorderBrush="Transparent"
                                Width="220" Height="380">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" Opacity="0.1" BlurRadius="5"/>
                            </Border.Effect>

                            <Grid>
                                <ContentPresenter />
                                <Border x:Name="SelectionBorder" 
                                        CornerRadius="8" 
                                        BorderThickness="3" 
                                        BorderBrush="#3F51B5" 
                                        Visibility="Hidden"/>
                            </Grid>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="MouseOver">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.BlurRadius)" To="15" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="CardBorder" Storyboard.TargetProperty="(Border.Effect).(DropShadowEffect.Opacity)" To="0.3" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SelectionStates">
                                    <VisualState x:Name="Unselected"/>
                                    <VisualState x:Name="Selected">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="SelectionBorder" Storyboard.TargetProperty="Visibility">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="{x:Static Visibility.Visible}"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Cartelera Disponible" 
                       FontSize="28" FontWeight="SemiBold" 
                       Foreground="#333"/>
            <TextBlock Text="Selecciona una función para continuar" 
                       FontSize="14" Foreground="#777" Margin="0,5,0,0"/>
        </StackPanel>

        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding ListaFunciones}" 
                 SelectedItem="{Binding FuncionSeleccionada}"
                 ItemContainerStyle="{StaticResource FunctionCardStyle}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Background="Transparent"
                 BorderThickness="0">

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" IsItemsHost="True"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="240"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Image Grid.Row="0" 
                               Source="{Binding Pelicula.PosterUrl, Converter={StaticResource MiPosterConverter}}" 
                               Stretch="UniformToFill"
                               VerticalAlignment="Top"
                               RenderOptions.BitmapScalingMode="HighQuality"/>

                        <Border Grid.Row="0" VerticalAlignment="Bottom" Height="60" Background="#99000000">
                            <TextBlock Text="{Binding Pelicula.Genero}" 
                                        Foreground="White" 
                                        VerticalAlignment="Center" 
                                        HorizontalAlignment="Center"
                                        FontSize="12" FontStyle="Italic"/>
                        </Border>

                        <StackPanel Grid.Row="1" Margin="15,10,15,5">
                            <TextBlock Text="{Binding Pelicula.Titulo}" 
                                       FontSize="16" FontWeight="Bold" 
                                       TextTrimming="CharacterEllipsis" 
                                       Foreground="#333"
                                       Height="42"/>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <TextBlock Text="📅" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Horario, StringFormat={}{0:hh\\:mm}}" 
                                           FontWeight="SemiBold" Foreground="#555"/>

                                <TextBlock Text="  |  " Foreground="#CCC"/>

                                <TextBlock Text="🎬" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Sala.Nombre}" Foreground="#555"/>
                            </StackPanel>
                        </StackPanel>

                        <Border Grid.Row="2" 
                                Background="#F0F0F0" 
                                CornerRadius="0,0,8,8" 
                                Padding="15,10">
                            <DockPanel>
                                <TextBlock Text="Precio:" VerticalAlignment="Center" Foreground="#777"/>
                                <TextBlock Text="{Binding PrecioTicket, StringFormat={}{0:C}}" 
                                           HorizontalAlignment="Right" 
                                           FontSize="18" FontWeight="Bold" 
                                           Foreground="#2E7D32"/>
                            </DockPanel>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="2" Padding="10" Background="White" BorderBrush="#EEE" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Función seleccionada:" 
                           VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#555"/>

                <Button Command="{Binding FinalizarVentaCommand}"
                        Content="SELECCIONAR ASIENTOS >"
                        Padding="20,10"
                        Background="#3F51B5" 
                        Foreground="White"
                        FontSize="14" FontWeight="Bold"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>