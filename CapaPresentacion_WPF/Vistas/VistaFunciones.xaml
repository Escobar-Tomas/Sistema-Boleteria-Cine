<UserControl x:Class="CapaPresentacion_WPF.Vistas.VistaFunciones"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             x:Name="Root">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20" Background="#F4F4F4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="20" Margin="0,0,20,0" BorderBrush="#DDD" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Nueva Función" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" Foreground="#007ACC"/>

                    <TextBlock Text="Película"/>
                    <ComboBox ItemsSource="{Binding ListaPeliculas}" SelectedItem="{Binding PeliculaSeleccionada}" DisplayMemberPath="Titulo"/>

                    <TextBlock Text="{Binding PeliculaSeleccionada.DuracionMinutos, StringFormat='Duración: {0} min'}" 
                               FontSize="11" Foreground="Gray" Margin="0,-10,0,15" FontStyle="Italic"/>

                    <TextBlock Text="Sala"/>
                    <ComboBox ItemsSource="{Binding ListaSalas}" SelectedItem="{Binding SalaSeleccionada}" DisplayMemberPath="Nombre"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Formato"/>
                            <ComboBox ItemsSource="{Binding ListaFormatos}" SelectedItem="{Binding FormatoSeleccionado}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="Idioma"/>
                            <ComboBox ItemsSource="{Binding ListaIdiomas}" SelectedItem="{Binding IdiomaSeleccionado}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Fecha de la Función"/>
                    <DatePicker SelectedDate="{Binding FechaEntrada}" Margin="0,0,0,15" Padding="5"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="Hora (HH:mm)"/>
                            <TextBox Text="{Binding HoraEntrada}" TextAlignment="Center" ToolTip="Ejemplo: 18:30"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="Precio ($)"/>
                            <TextBox Text="{Binding PrecioEntrada}" TextAlignment="Right"/>
                        </StackPanel>
                    </Grid>

                    <Button Content="PROGRAMAR FUNCIÓN" Command="{Binding CrearFuncionCommand}" 
                            Height="40" Margin="0,10,0,0" 
                            Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="White" Padding="10" CornerRadius="5" Margin="0,0,0,10" BorderBrush="#DDD" BorderThickness="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Ver cartelera del día:" VerticalAlignment="Center" Margin="0,0,10,5"/>
                    <DatePicker SelectedDate="{Binding FechaFiltro}" Width="120" VerticalContentAlignment="Center"/>
                    <Button Content="Actualizar" Command="{Binding CargarFuncionesCommand}" Margin="10,0,0,0" Padding="10,5" Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Border>

            <DataGrid Grid.Row="1" ItemsSource="{Binding ListaFunciones}" AutoGenerateColumns="False" 
                      IsReadOnly="True" CanUserAddRows="False" Background="White" RowHeight="40"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" BorderBrush="#DDD" BorderThickness="1">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Hora" Binding="{Binding FechaHoraInicio, StringFormat=HH:mm}" Width="60" FontWeight="Bold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Película" Binding="{Binding Pelicula.Titulo}" Width="*"/>

                    <DataGridTextColumn Header="Sala" Binding="{Binding Sala.Nombre}" Width="100"/>
                    <DataGridTextColumn Header="Formato" Binding="{Binding Formato}" Width="70"/>
                    <DataGridTextColumn Header="Idioma" Binding="{Binding Idioma}" Width="90"/>

                    <DataGridTextColumn Header="Fin Aprox." Binding="{Binding FechaHoraFin, StringFormat=HH:mm}" Width="70" Foreground="Gray"/>

                    <DataGridTextColumn Header="Precio" Binding="{Binding PrecioTicket, StringFormat='${0:N0}'}" Width="80"/>

                    <DataGridTemplateColumn Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Borrar" FontSize="10" Padding="5,2" Background="#FFEBEE" Foreground="Red" BorderThickness="0" Cursor="Hand"
                                        Command="{Binding DataContext.EliminarFuncionCommand, ElementName=Root}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>