<UserControl x:Class="CapaPresentacion_WPF.Vistas.VistaUsuarios"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="10" Margin="0,0,20,0">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Detalles del Usuario" FontSize="20" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Text="Complete la información para registrar" FontSize="12" Foreground="#888"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Nombre Completo" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NombreCompletoForm, UpdateSourceTrigger=PropertyChanged}" 
                             Height="35" Padding="5" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

                    <TextBlock Text="Nombre de Usuario" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding NombreUsuarioForm, UpdateSourceTrigger=PropertyChanged}" 
                             Height="35" Padding="5" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

                    <TextBlock Text="Rol Asignado" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding ListaRoles}" 
                              SelectedItem="{Binding RolForm}"
                              Height="35" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

                    <TextBlock Text="Contraseña" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="txtPassword"
                                 Height="35" Padding="5" VerticalContentAlignment="Center" Margin="0,0,0,5"
                                 PasswordChanged="txtPassword_PasswordChanged"/>
                    <TextBlock Text="Dejar vacío para mantener la actual (en edición)" 
                               FontSize="10" Foreground="#999" Margin="0,0,0,15"/>

                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
                    <Button Command="{Binding GuardarCommand}" Content="GUARDAR USUARIO" 
                            Height="40" Background="#3F51B5" Foreground="White" FontWeight="Bold"
                            BorderThickness="0" Margin="0,0,0,10" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Command="{Binding LimpiarFormularioCommand}" Content="Cancelar / Limpiar" 
                            Height="35" Background="Transparent" Foreground="#555" BorderBrush="#CCC" BorderThickness="1"
                            Cursor="Hand" Click="btnLimpiar_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Lista de Usuarios Registrados" 
                       FontSize="22" FontWeight="SemiBold" Foreground="#333" Margin="0,0,0,15"/>

            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding ListaUsuarios}" 
                      SelectedItem="{Binding UsuarioSeleccionado}"
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      Background="White"
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#EEE"
                      RowHeight="40">

                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Background" Value="#F9F9F9"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.05" BlurRadius="5"/>
                </DataGrid.Effect>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre Completo" Binding="{Binding NombreCompleto}" Width="*"/>

                    <DataGridTextColumn Header="Usuario" Binding="{Binding NombreUsuario}" Width="150"/>

                    <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="120"/>

                    <DataGridTemplateColumn Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.EliminarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                    CommandParameter="{Binding}"
                    ToolTip="Eliminar Usuario"
                    Background="#FFEBEE" 
                    Foreground="#D32F2F" 
                    BorderThickness="0"
                    Width="30" Height="30" 
                    HorizontalAlignment="Center"
                    Content="🗑">

                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="15"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>